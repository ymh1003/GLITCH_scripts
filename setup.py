from setuptools import setup

setup(
    name='GLITCH-scripts',
    version='0.0.1',
    install_requires=[],
    scripts=['glitch_runner.py', 'oa_bbox.py', 'stlrotate', 'stlscale', 'visualize_pcd.py']
)
